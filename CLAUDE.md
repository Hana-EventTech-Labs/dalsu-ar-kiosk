# Dalsu AR Kiosk — 삼성 달수 AR 포토카드 키오스크

## 프로젝트 개요
삼성전자 주관 **대한민국 국제물주간(KIWW, 대구 엑스코, 2026-09-09~11)** 행사용 무인 AR 포토카드 키오스크.
관람객이 물방울 4개(삼성 4대 환경목표)를 터치 → S자 물길 완성 → 자연 회복 연출 → 달수 유영 → 3초 카운트다운 자동 촬영
→ 달수·물길·자연 AR 합성 → 미리보기 중 Smart-81D 양면 포토카드 출력. 체험 50초~1분 20초.

- 견적: quote-maker `QT-20260824-001` (개발 800K + 그래픽 제작 500K, VAT 별도)
- 라인: 스튜디오 머그음(유지환) → 비전컴퍼니(info@vscompany.co.kr) → 하나플랫폼(사업팀 정두용 → 개발팀)
- 납기 2026-09-04(조정 요청 중, 원래 9/2). 현장 리허설 후 9/9 오픈.
- 하드웨어: 32인치 터치 키오스크(세로) + 상단 웹캠 + **Smart-81D 양면 카드프린터**(플리퍼)

## 요구사항 (2026-08-25 머그음 기획 확정본 — 단일 출처)
1. 대기 화면 → 물방울 4개 등장 (탄소 / 수자원 / 폐기물 / 오염물질)
2. 달수 손/실루엣 가이드가 터치 유도
3. 터치 시 POP 효과 + 환경목표 문구 표시
   - 탄소: 2050년 탄소중립 달성
   - 수자원: 2030년 용수 취수량 2021년 수준으로 절감
   - 폐기물: 2030년 폐기물 재활용률 99.9% 달성
   - 오염물질: 2040년 대기·수질 오염물질 자연상태 수준으로 저감
4. 4개 완료 → **4개의 물줄기(지류)가 물방울 자리에서 흘러나와 화면 중앙에서 하나로 합류** → 그 자리에서 **삼성의 S자** 물길이 위·아래 양쪽으로 뻗어 완성 (2026-08-26 머그음 추가 확인)
5. **달수가 물길을 헤엄쳐 내려가며 지나온 자리에 자연이 살아난다** (식물·물고기·잠자리·숲, 동화풍) — 2026-08-27 확정.
   자연을 먼저 다 피워놓고 달수가 지나가는 것이 아니라, **달수의 진행이 생명을 번지게 하는 원인**이어야 한다.
6. 달수가 물길 따라 **헤엄쳐** 이동 → **중앙 도착 시 촬영 화면이 켜진다**. 포즈는 옷 입은 엎드림(`dalsu-float.png`)
7. 중앙 도착 → 3·2·1 카운트다운 → 자동 촬영
8. 촬영 사진 + 달수·물길·자연 AR 합성 → 미리보기
9. 미리보기 동안 포토카드 출력(약 25초) — 별도 대기화면 없음. **앞면 = 합성 사진, 뒷면 = 고정 디자인**
9-1. **달수는 옷 입은(탐험가 복장) 버전으로 통일**한다 (2026-08-26 확정). 원본 `assets-src/dalsu-explorer-turnaround.ai.pdf`(서기 4방향)와 `dalsu-lying-2.ai.pdf` 우측(엎드림)에서 뽑는다.
10. 시안 8컷의 **레이아웃·인터랙션 구조는 그대로 따른다**(2026-08-26 확정): 상단 고정 타이틀/부제, 가로 1열 눈물방울 4개, 손가락 커서 가이드, 터치 시 그 자리에 목표 문구 잔류, 완료 화면의 프린터 일러스트.
    단 **룩(파란 3D 수달·실사 배경)은 재현하지 않는다** — 달수는 원본 .ai 5종(플랫 2D), 배경·자연은 벡터 동화풍.
11. 시안과 의도적으로 다른 2가지: ① 촬영 화면(카운트다운·촬영)에는 달수·카메라 아이콘을 넣지 않는다(사람이 가려짐) ② 카운트다운은 작게 상단 배치.

## 아키텍처
```
dalsu-ar-kiosk/
├── kiosk/                  # Electron 앱 (터치 UI · 웹캠 · AR 합성)
│   ├── main.js             # 창 생성, IPC(print/log/config), 인쇄 CLI 스폰, 출력 파일 저장
│   ├── preload.js          # window.kiosk 브리지
│   ├── config.json         # 타이밍·문구·프린터 모드·카메라 (현장에서 수정하는 유일한 파일)
│   ├── src/
│   │   ├── index.html / styles.css
│   │   ├── flow.js         # 상태 머신 (순수 JS, 테스트 대상)
│   │   ├── compose.js      # 합성 레이아웃 계산 (순수 JS, 테스트 대상)
│   │   ├── river.js        # S자 물길 경로·달수 이동 좌표 (순수 JS)
│   │   └── renderer.js     # DOM/캔버스/웹캠/애니메이션 — flow/compose/river 사용. ?smoke=1 이면 자동 실행(모의 프레임 → front.png → exit)
│   └── assets/             # dalsu-*.png(변환본), card-back.png, bubble/nature 그래픽
├── printer/DalsuPrint/     # C# .NET 8 콘솔 CLI — SmartComm2 SDK 양면 인쇄
│   ├── Program.cs          # --front --back [--printer desc] [--list] [--dry-run]
│   ├── SmartComm2Wrapper.cs# smart51s-nfc-writer에서 검증된 래퍼 그대로
│   └── SmartComm2.dll
├── scripts/
│   ├── convert-ai.ps1      # assets-src/*.ai(.pdf) → kiosk/assets/*.png (WinRT PDF 렌더, 투명 배경)
│   ├── make-card-back.ps1  # 뒷면 카드 기본 디자인 생성 (1012×636)
│   └── build-printer.ps1   # dotnet publish → printer/dist/DalsuPrint.exe
├── tests/                  # node --test (flow/compose/river 순수 모듈)
├── assets-src/             # 달수 원본 .ai (PDF 호환) — 수정 금지
├── out/                    # 촬영 결과 (날짜별 front/back PNG) — gitignore
└── logs/                   # 앱·인쇄 로그 — gitignore
```

## 핵심 불변식
- **카드는 CR-80(Smart-31/51/81 공통), SDK 카드 좌표 `664×1040`(세로).** 방향은 `config.card.orientation`
  (`portrait` = 664×1040 기본 / `landscape` = 1040×664). 앞면·뒷면·미리보기가 모두 같은 방향이어야 한다.
  **이 값은 라테일 팝업스토어(`Hana-EventTech-Labs/latale-popup` → `03-character-card-kiosk`)에서 SMART-81 양면 풀블리드 인쇄로 실측 확인된 규격이다.** 임의로 바꾸지 말 것.
  뒷면 `card-back.png` 도 `npm run assets` 가 config 방향을 따라 만든다.
- **인쇄 SDK 경로는 `config.printer.sdk`**: **`comm`(기본)** = 드라이버(스풀러) 모드 + **`dwPrtSide` 직접 패치로 양면 강제** — 라테일에서 실장비 양면 출력 확인.
  `dcl` = 직접통신 SBS 시퀀스(폴백). **라테일 기록상 드라이버 설치 환경에서 `SmartDCL` 은 IO 실패했다** — 현장에서 `comm` 이 안 되면 그때만 `dcl` 로 바꾼다.
  양면 강제 오프셋은 `(len-12976)+164`, 값 0=앞면/2=양면. 레이아웃이 다르거나 현재 값이 3 초과면 **패치하지 않는다**(안전 가드). 자세한 계획은 `docs/PRINTER_81D_PLAN.md`.
- 인쇄 시퀀스(검증됨): `SBSStart → CardIn → Move(PRINT) → DrawBitmap(front) → DrawBitmap(back) → DCLPrint(BOTH) → DoPrint → DoPrint → CardOut → SBSEnd`
- 뒷면은 플리퍼 장착 시에만 그린다. 미장착이면 앞면만 인쇄하고 경고.
- SmartComm2 **RF 함수 호출 금지**(네이티브 크래시). 이 프로젝트는 NFC 없음.
- 인쇄 실패는 사용자 화면에 "직원 호출" 안내 + 로그. 자동 재시도 1회.
- 프린터 모드 `config.printer.mode`: `smart`(실장비) | `dry-run`(PNG만 저장, 개발 PC). 기본 dry-run. 현장 배포 전 반드시 `smart`로 전환.
- 모든 문구·타이밍은 `kiosk/config.json`에서만 바꾼다. 코드에 하드코딩 금지.
- **물길은 굽이치는 시냇물**(2026-08-26 확정): 글자 S를 그대로 그리면 도형처럼 뻣뻣해진다. 전체 실루엣이 자연스럽게 S를 이루면 된다.
  `u=0` = 위 가운데 **합류점**(0.50, 0.38), `u=0.5` = 달수 도착점(0.50, 0.66), `u=1` = 좌하단 화면 밖(0.28, 1.12).
  폭은 원근(아래가 굵음) × 여울·소(폭이 일정하지 않음), 둑은 좌우가 따로 들쭉날쭉(`water.bankWobble`). 매끈한 리본으로 되돌리지 말 것.
  경로는 `config.river.path`로 코드 수정 없이 덮어쓸 수 있다(`river.setPath`).
- **4줄기 합류**(기획 4·5번): "4개의 물길이 **위에서 아래로 하나로 뭉쳐지면서** S자 형태로 간다."
  상단 줄로 올라간 각 목표 문구 아래에서 물줄기가 나와 **물길 머리(u=0) 한 점**으로 모인다(`river.tributaries(sources, pointAt(0))`).
  합류점을 물길 **중간**에 두면 지류가 본류를 가로질러야 해서 합쳐지는 느낌이 사라진다 — 반드시 머리여야 한다.
  본류는 합류점(u=0)에서 하류로 **한 방향**으로 그려진다. 지류는 자연 회복 단계에 가서야 본류에 흡수된다(그 전에 지우면 "없어지고 딴 게 생겼다"로 보인다). 합류 순간 목표 문구 4개는 **사라지지 않고 상단 한 줄(`screen.goalRowTop`)로 올라가 남는다** — 시안 5컷의 "4개의 목표가 모여 하나의 깨끗한 물길이 됩니다"는 4개가 보여야 성립한다. 자연 회복(6컷)으로 넘어갈 때 페이드아웃.
- **자연 회복은 달수의 진행에 종속된다**: `anim.life = (swimP - 0.06) × 1.18` 로 달수보다 살짝 뒤처진 '생명의 앞머리'를 만들고, 자연 요소와 나무를 그 값으로 등장시킨다. 숲 슬롯은 **y 오름차순으로 정렬**해야 위→아래(달수가 가는 방향)로 살아난다.
  NATURE 단계는 자연을 피우는 구간이 아니라 **달수가 물길 머리에 떠오르는 짧은 비트**다(`anim.enter`).
  ⚠ 물을 탁하게 했다가 맑아지게 하는 연출은 **넣지 말 것** — 5컷 문구가 "하나의 **깨끗한** 물길이 됩니다"라 정면으로 모순된다(2026-08-27 시도 후 철회).
- **실사 자연 자산**(2026-08-27): `kiosk/assets/` 에 **종류별로** 넣는다 — `fish-*.png`(물 위) / `plant-*.png`(물가) / `tree-*.png`(숲).
  한 묶음(`nature-*`)으로 두면 물가에 물고기가 놓인다. 파일이 없으면 `art.js` 벡터 스프라이트로 자동 폴백.
  **물고기는 달수를 가리지 않는다** — `drawNature(..., avoid)` 가 달수 반경 안의 물고기를 비켜준다.
  현재 **물고기 4종만 실사**(Wikimedia Commons, PD/CC0). 나무·물가 식물은 벡터 — 라이선스가 명확한 실사 컷아웃을 못 구했고,
  세밀화 도판은 캡션째 들어오고 잎/꽃 사진은 벡터 물길과 톤이 맞지 않았다(시도 후 철회).
  ⚠ 외부 이미지를 추가하면 반드시 `scripts/cutout-web-assets.py` 로 처리하고 `docs/ASSET_CREDITS.md` 에 출처·라이선스를 남긴다. 테스트가 이를 검사한다.
- **숲과 언덕**(시안 5·6컷): 물길만 있으면 강이 흰 배경에 떠 있다. 원경 언덕은 물길과 함께(RIVER), 나무는 자연 회복(NATURE) 때 하나씩 자란다.
  나무 자리는 `river.scenerySlots()` 가 **물길에서 `scene.minDist` 이상 떨어진 곳**만 고른다(수관이 강을 침범하면 안 됨). 배치는 결정적이라 현장에서 매번 같은 그림이 나온다.
  갈대는 물가 전용이라 숲 목록(`ART_TREE_ORDER`)에 넣지 않는다 — 넣으면 공중에 뜬다.
  카드에도 같은 나무를 양 가장자리에 세워 화면과 카드를 한 세계로 묶는다(`card.trees`).
- **강폭은 원근**: 먼 쪽(위)이 가늘고 가까운 쪽(아래)이 굵은 **단조 증가**(`water.widthAt`). 종 모양(가운데가 최대)은 원근 위반이라 "강"이 아니라 "리본"으로 읽힌다 — 되돌리지 말 것.
- **좌표계 규칙 (중요)**: `getBoundingClientRect()` 는 **뷰포트 절대 좌표**다. `#fx` 캔버스와 `#stage` 안의 요소는 **무대 좌표계**를 쓰므로 반드시 무대 좌상단 오프셋을 빼야 한다 — `renderer.js` 의 `elCenter()` 를 쓸 것.
  세로 키오스크에서는 오프셋이 0이라 티가 안 나지만, **가로 모니터에서는 무대가 가운데 레터박스로 놓여 물줄기·손가락 커서·물방울 터짐 좌표가 전부 화면 밖으로 밀린다.**
  `npm run build:demo` 의 **e2e 검증**(세로·가로 2회, 각 회차마다 **관람객 2명분 연속 진행**)이 이걸 잡는다 — 발원지가 화면 안인지, 메뉴 순서대로인지, 화면 폭에 고르게 퍼졌는지, 그리고 **실제 픽셀에 물이 그려졌는지**까지 본다.
- **성능 (GPU 없는 현장 PC 대비, 2026-08-27)**: 물 연출은 소프트웨어 렌더링에서 가장 무겁다. 아래를 되돌리지 말 것.
  · `fx` 캔버스를 **매 프레임 재할당하지 않는다**(크기·배율이 바뀔 때만). 1080×1920이면 프레임마다 8MB를 다시 잡는 셈이라 가장 비쌌다.
  · `ctx.filter = blur()` 는 매번 전체 서피스를 필터링한다. 저품질에서는 `water.softStroke()`(알파 낮춘 다중 스트로크)로 대체한다.
  · `PERF_STEPS` 4단계 자동 강등: 기본 → 흐림 끔(44구간) → 렌더 배율 0.6 → 0.45. **프레임 평균 28ms 초과가 1.5초 이상 지속되면 한 칸씩 내려간다.**
  · 시작 로그에 `그래픽 가속: 하드웨어/소프트웨어` 와 `렌더 성능 {fps, step, scale}` 이 남는다 — 현장 진단의 첫 단서.
  · `config.river.quality`: `auto`(기본) / `high` / `low`(2단계로 고정).
- **화면은 항상 9:16 무대 안에**: `#stage`가 `min(100vw, 56.25vh) × min(100vh, 177.78vw)` + `container-type: size`.
  무대 안 길이 단위는 뷰포트(`vw`/`vh`)가 아니라 **무대 기준(`cqw`/`cqh`)** 을 쓴다 — 가로 모니터에서 `vw`로 잡으면 물방울이 커지며 하단 달수를 덮는다. 인라인 스타일(JS)도 `cqw`/`cqh`.
  빌드 게이트가 `--smoke-emulate=1080x1920`(실기 해상도 CSS 레이아웃)과 `--smoke-size=960x540`(가로 비율)을 함께 검증한다.
  `--smoke-emulate` 는 창을 키우지 않고 뷰포트만 강제하므로, 개발 모니터가 작아도 실제 키오스크 레이아웃을 그대로 볼 수 있다.
- **손가락 커서는 가이드 단계 내내 보인다.** 4개를 다 눌러도 숨기지 않는다 — 가이드 화면이 넘어갈 때 섹션째 사라진다.
  물방울을 새로 만든 직후에는 레이아웃이 확정되기 전이라 좌표가 0일 수 있으므로 `requestAnimationFrame` 뒤에 배치한다.
- **데드맨 스위치**: 어떤 상태든 예산의 2배(최소 20초) 동안 진행이 없으면 대기 화면으로 강제 복귀. `idleReturnMs`는 GUIDE에서만 동작하므로 그 밖의 상태를 이게 덮는다.
- 촬영 직전 `timing.readyMs` 동안 "준비하세요"를 띄운다. 곧바로 카메라를 켜면 놀란 표정으로 찍힌다.
- 강물은 `water.js`의 **흐르는 수면**(텍스처를 흐름 방향으로 스크롤). 흰 테두리 + 점선 대시 스트로크는 유아틱해서 폐기 — `config.river.style: "cartoon"`으로만 되돌릴 수 있다.
  수면 텍스처는 절차 생성이 기본이고, `kiosk/assets/water.png`(흐름 방향 = 가로, 좌우 타일러블)를 넣으면 그 이미지로 교체된다.
- 물방울은 **가로 1열 4개, 눈물방울 모양**. 터치하면 물방울이 사라지고 **그 자리에 목표 문구가 남는다**(누적, 시안 3~4컷).
- 달수 **헤엄 위상은 전진 위상과 같은 값**을 쓴다: `θ = swimP × SWIM_STROKES × 2π`. 시간 기반으로 따로 돌리면 몸짓과 이동이 어긋나 허우적거려 보인다.
  더 나아가 **물 차는 힘의 위상까지 맞춰야 한다**: `swimEase` 의 속도는 `1-0.6cos(θ)` 라 **θ=π 에서 최대**이므로 `push = max(0, -cos(θ))`.
  `sin(θ)` 를 쓰면 90° 어긋나 "가장 빨리 나아가는 순간에 몸은 쉬고 있는" 그림이 된다. `tests/storyboard.test.js` 가 이 위상 일치를 검사한다.
  달수는 **물길 폭 안에 들어와야** 한다 — 물길보다 크면 물길을 덮어 잔디 위에 있는 것처럼 보인다(`river.widthRatio` ↔ 달수 크기 함께 조정).
- 달수 이동은 **헤엄**(motion.js `swimEase`/`swimPose` — 차고-미끄러짐 + 몸통 흔들림). 등속 슬라이드 금지. 도착점은 **물길 중앙 u=0.5**.
- **카드 앞면 AR = 「복구된 자연(강물 제외) + 달수」만**(2026-08-26 확정). 강물·수면은 화면 연출 전용이며 카드에 넣지 않는다.
  자연은 `compose.natureCardSlots()` 의 세 구역(왼쪽 물가 / 인물 앞 전경 / 달수 왼쪽 틈)에만 앉히고, 하단에 전경 풀을 깐다.
  피해야 할 것: ① 가운데 인물(얼굴·상반신) ② 우하단 달수. 나는 것(잠자리)은 접지 그림자를 그리지 않는다.
- **디자인은 진하게, 글씨는 볼드**: 번들 서체(Jua·Gowun Dodum)에 볼드 웨이트가 없으므로 **Jua로 통일 + `font-weight:700`(합성 볼드) + 외곽 보강**으로 굵기를 만든다.
  본문 서체를 Gowun Dodum(얇음)으로 되돌리지 말 것. 물방울 라벨은 흰색(진한 물방울 위 대비), 목표 문구는 `#063a5c` + 흰 외곽.
- `BrowserWindow`는 `backgroundThrottling: false` 필수. 켜져 있으면 창이 가려질 때 rAF가 멈춰 연출이 정지한다.
- **촬영 화면(COUNTDOWN·CAPTURE)은 비운다**: 카메라 위에 달수·물길·자연을 겹치지 않는다(사람이 가려짐). AR 합성은 출력 카드에서만. `screen.captureOverlay: true`로만 예외 허용.

## 명령
```bash
npm install                 # kiosk 의존성 (electron)
npm test                    # 순수 모듈 테스트 (node --test)
npm run smoke               # 웹캠 없이 전 흐름 자동 실행 → out/smoke/front.png + dry-run 인쇄 → exit 0
npm start                   # 키오스크 실행 (창 모드)
npm run kiosk               # 전체화면 키오스크 모드
npm run build:printer       # DalsuPrint.exe 빌드 (dotnet publish)
npm run assets              # .ai → PNG 변환(+파편 제거) + 뒷면 카드 생성
printer/dist/DalsuPrint.exe --list                       # 프린터 목록
printer/dist/DalsuPrint.exe --front a.png --back b.png   # 실제 인쇄
```

## 완료 게이트 (보고 전 필수)
1. `npm test` 전부 통과
2. `npm run smoke` exit 0 + `out/smoke/front.png` 1012×636 생성 확인
3. `npm run build:printer` 성공 + `DalsuPrint.exe --dry-run --front ... --back ...` exit 0
4. 실장비 인쇄는 Smart-81D 연결된 PC에서만 검증 가능 — 미검증 시 "미검증"으로 명시 보고

## 컨벤션
- JS: CommonJS, 순수 로직은 DOM 의존 없이 분리(테스트 가능). 한글 주석 OK.
- C#: 기존 NFC Writer 코드 스타일 유지. 로그는 stdout, 오류는 stderr + exit code(0 성공/1 인자/2 SDK/3 프린터/4 인쇄).
- 커밋: conventional commits. 자동 커밋 금지.
- 자산 파일명 고정: `dalsu-front.png`, `dalsu-side.png`, `dalsu-float.png`, `dalsu-face.png`, `card-back.png`. 그래픽 교체는 파일만 덮어쓴다.
