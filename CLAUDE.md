# Dalsu AR Kiosk — 삼성 달수 AR 포토카드 키오스크

## 프로젝트 개요
삼성전자 주관 **대한민국 국제물주간(KIWW, 대구 엑스코, 2026-09-09~11)** 행사용 무인 AR 포토카드 키오스크.
관람객이 물방울 4개(삼성 4대 환경목표)를 터치 → S자 물길 완성 → 자연 회복 연출 → 달수 유영 → 3초 카운트다운 자동 촬영
→ 달수·물길·자연 AR 합성 → 미리보기 중 Smart-81D 양면 포토카드 출력. 체험 50초~1분 20초.

- 견적: quote-maker `QT-20260824-001` (개발 800K + 그래픽 제작 500K, VAT 별도)
- 라인: 스튜디오 머그음(유지환) → 비전컴퍼니(info@vscompany.co.kr) → 하나플랫폼(사업팀 정두용 → 개발팀)
- 납기 2026-09-04(조정 요청 중, 원래 9/2). 현장 리허설 후 9/9 오픈.
- 하드웨어: 32인치 터치 키오스크(세로) + 상단 웹캠 + **Smart-81D 양면 카드프린터**(플리퍼)

## 콘텐츠 수정 (2026-09-01 클라이언트 `(FM)KIWW2026_AR포토부스_콘텐츠수정.pptx` — 아래 요구사항 3·4번을 덮어쓴다)
- 4대 목표 → **물 순환 4단계 Reduce / Reuse / Recycle / Return**. 터치 시 문구: "물 사용량을 줄이고" / "물 재이용을 늘리고" /
  "정수 기술로 재활용하여" / "깨끗하게 정화된 물을 자연에게 돌려줍니다". 4개 완료 캡션: "다시 쓰는 물, 다시 살아나는 물길 / 그리고, 그 물길 끝에서 만나는 달수".
  엔딩·촬영 이후는 동일. `config.goals` 의 key 도 `reduce/reuse/recycle/return` 으로 바뀌었다(테스트 참조).
- 부제(`screen.headSub`) "물을 아끼고, 다시 쓰고, 재활용하고, 자연에 돌려주는 / 물 순환의 4단계"는 **우리 초안** — pptx 에 없음, 클라이언트 확인 필요.
- **서체 전면 삼성 서체**: `Samsung SS Head KR`(제목, 700/500) + `Samsung SS Body KR`(본문, 700/400), 디지털용 WOFF2 4개를 `kiosk/assets/fonts/samsung/` 에 동봉.
  플레이북 기준 18pt 이하 본문은 Body. 서체 배포 zip 원본은 카카오톡 수신 폴더 `[2026] 신규 서체 전달.zip`. Jua·Gowun Dodum 파일은 남아 있지만 더는 참조하지 않는다.
- ⚠ **그래픽 톤을 참고 영상(삼성 반도체 물발자국, 흰 배경 아이소메트릭)에 맞추라는 요청은 미반영** — 8/25·8/26 확정 방향(플랫 2D 달수 + 동화풍)과 다른 재제작이라 일정·범위 협의 중.

## 요구사항 (2026-08-25 머그음 기획 확정본 — 단일 출처)
1. 대기 화면 → 물방울 4개 등장 (탄소 / 수자원 / 폐기물 / 오염물질)
2. 달수 손/실루엣 가이드가 터치 유도
3. 터치 시 POP 효과 + 환경목표 문구 표시
   - 탄소: 2050년 탄소중립 달성
   - 수자원: 2030년 용수 취수량 2021년 수준으로 절감
   - 폐기물: 2030년 폐기물 재활용률 99.9% 달성
   - 오염물질: 2040년 대기·수질 오염물질 자연상태 수준으로 저감
4. 4개 완료 → **4개의 물줄기(지류)가 물방울 자리에서 흘러나와 화면 중앙에서 하나로 합류** → 그 자리에서 **삼성의 S자** 물길이 위·아래 양쪽으로 뻗어 완성 (2026-08-26 머그음 추가 확인)
5. **달수가 물길을 헤엄쳐 내려가며 지나온 자리에 자연이 살아난다** (식물·물고기·잠자리·숲, 동화풍) — 2026-08-27 확정.
   자연을 먼저 다 피워놓고 달수가 지나가는 것이 아니라, **달수의 진행이 생명을 번지게 하는 원인**이어야 한다.
6. 달수가 물길 따라 **헤엄쳐** 이동 → **중앙 도착 시 촬영 화면이 켜진다**. 포즈는 옷 입은 엎드림(`dalsu-float.png`)
7. 중앙 도착 → 3·2·1 카운트다운 → 자동 촬영
8. 촬영 사진 + 달수·물길·자연 AR 합성 → 미리보기
9. 미리보기 동안 포토카드 출력(약 25초) — 별도 대기화면 없음. **앞면 = 합성 사진, 뒷면 = 고정 디자인**
9-1. **달수는 옷 입은(탐험가 복장) 버전으로 통일**한다 (2026-08-26 확정). 원본 `assets-src/dalsu-explorer-turnaround.ai.pdf`(서기 4방향)와 `dalsu-lying-2.ai.pdf` 우측(엎드림)에서 뽑는다.
10. 시안 8컷의 **레이아웃·인터랙션 구조는 그대로 따른다**(2026-08-26 확정): 상단 고정 타이틀/부제, 가로 1열 눈물방울 4개, 손가락 커서 가이드, 터치 시 그 자리에 목표 문구 잔류, 완료 화면의 프린터 일러스트.
    단 **룩(파란 3D 수달·실사 배경)은 재현하지 않는다** — 달수는 원본 .ai 5종(플랫 2D), 배경·자연은 벡터 동화풍.
11. 시안과 의도적으로 다른 2가지: ① 촬영 화면(카운트다운·촬영)에는 달수·카메라 아이콘을 넣지 않는다(사람이 가려짐) ② 카운트다운은 작게 상단 배치.

## 아키텍처
```
dalsu-ar-kiosk/
├── kiosk/                  # Electron 앱 (터치 UI · 웹캠 · AR 합성)
│   ├── main.js             # 창 생성, IPC(print/log/config), 인쇄 CLI 스폰, 출력 파일 저장
│   ├── preload.js          # window.kiosk 브리지
│   ├── config.json         # 타이밍·문구·프린터 모드·카메라 (현장에서 수정하는 유일한 파일)
│   ├── src/
│   │   ├── index.html / styles.css
│   │   ├── flow.js         # 상태 머신 (순수 JS, 테스트 대상)
│   │   ├── compose.js      # 합성 레이아웃 계산 (순수 JS, 테스트 대상)
│   │   ├── river.js        # S자 물길 경로·달수 이동 좌표 (순수 JS)
│   │   └── renderer.js     # DOM/캔버스/웹캠/애니메이션 — flow/compose/river 사용. ?smoke=1 이면 자동 실행(모의 프레임 → front.png → exit)
│   └── assets/             # dalsu-*.png(변환본), card-back.png, bubble/nature 그래픽
├── printer/DalsuPrint/     # C# .NET 8 콘솔 CLI — SmartComm2 SDK 양면 인쇄
│   ├── Program.cs          # --front --back [--printer desc] [--list] [--dry-run]
│   ├── SmartComm2Wrapper.cs# smart51s-nfc-writer에서 검증된 래퍼 그대로
│   └── SmartComm2.dll
├── scripts/
│   ├── convert-ai.ps1      # assets-src/*.ai(.pdf) → kiosk/assets/*.png (WinRT PDF 렌더, 투명 배경)
│   ├── make-card-back.ps1  # 뒷면 카드 기본 디자인 생성 (1012×636)
│   ├── make-swim-plate.py  # 헤엄 사이클 생성용 크로마 플레이트 (크레딧 0)
│   ├── grab-frames.js/.html# 영상 → PNG 프레임 (ffmpeg 없이 Electron, 균등 간격 시킹)
│   ├── key-swim-sheet.py   # 크로마 키잉·디스필·사이클 절단·실루엣 정합 → dalsu-swim.png/.json
│   ├── verify-swim-sheet.py# 시트 자동 검증 16종 + phase0 측정 + contact-sheet
│   ├── cutout-nature.py    # 단색 배경 자연 스트립 → tree-*.png / plant-*.png (테두리 flood fill)
│   └── build-printer.ps1   # dotnet publish → printer/dist/DalsuPrint.exe
├── tests/                  # node --test (flow/compose/river 순수 모듈)
├── assets-src/             # 달수 원본 .ai (PDF 호환) — 수정 금지
├── out/                    # 촬영 결과 (날짜별 front/back PNG) — gitignore
└── logs/                   # 앱·인쇄 로그 — gitignore
```

## 핵심 불변식
- **카드는 CR-80(Smart-31/51/81 공통), SDK 카드 좌표 `664×1040`(세로).** 방향은 `config.card.orientation`
  (`portrait` = 664×1040 기본 / `landscape` = 1040×664). 앞면·뒷면·미리보기가 모두 같은 방향이어야 한다.
  **이 값은 라테일 팝업스토어(`Hana-EventTech-Labs/latale-popup` → `03-character-card-kiosk`)에서 SMART-81 양면 풀블리드 인쇄로 실측 확인된 규격이다.** 임의로 바꾸지 말 것.
  뒷면 `card-back.png` 도 `npm run assets` 가 config 방향을 따라 만든다.
- **인쇄 SDK 경로는 `config.printer.sdk`**: **`comm`(기본)** = 드라이버(스풀러) 모드 + **`dwPrtSide` 직접 패치로 양면 강제** — 라테일에서 실장비 양면 출력 확인.
  `dcl` = 직접통신 SBS 시퀀스(폴백). **라테일 기록상 드라이버 설치 환경에서 `SmartDCL` 은 IO 실패했다** — 현장에서 `comm` 이 안 되면 그때만 `dcl` 로 바꾼다.
  양면 강제 오프셋은 `(len-12976)+164`, 값 0=앞면/2=양면. 레이아웃이 다르거나 현재 값이 3 초과면 **패치하지 않는다**(안전 가드). 자세한 계획은 `docs/PRINTER_81D_PLAN.md`.
- 인쇄 시퀀스(검증됨): `SBSStart → CardIn → Move(PRINT) → DrawBitmap(front) → DrawBitmap(back) → DCLPrint(BOTH) → DoPrint → DoPrint → CardOut → SBSEnd`
- 뒷면은 플리퍼 장착 시에만 그린다. 미장착이면 앞면만 인쇄하고 경고.
- SmartComm2 **RF 함수 호출 금지**(네이티브 크래시). 이 프로젝트는 NFC 없음.
- 인쇄 실패는 사용자 화면에 "직원 호출" 안내 + 로그. 자동 재시도 1회.
- 프린터 모드 `config.printer.mode`: `smart`(실장비) | `dry-run`(PNG만 저장, 개발 PC). 기본 dry-run. 현장 배포 전 반드시 `smart`로 전환.
- 모든 문구·타이밍은 `kiosk/config.json`에서만 바꾼다. 코드에 하드코딩 금지.
- **물길은 굽이치는 시냇물**(2026-08-26 확정): 글자 S를 그대로 그리면 도형처럼 뻣뻣해진다. 전체 실루엣이 자연스럽게 S를 이루면 된다.
  `u=0` = 위 가운데 **합류점**(0.50, 0.38), `u=0.5` = 달수 도착점(0.50, 0.66), `u=1` = 좌하단 화면 밖(0.32, 1.12).
  경로는 `config.river.path` 에 실려 있다(2026-08-29, "삼성의 S 느낌으로" 요청). 좌우 굽이를 깊게(x 0.16 ↔ 0.86) 잡아 S 가 읽히게 했고,
  좌우 여유는 계산으로 확인했다(띠 바깥 0.144~0.910 — 무대 안). **다만 둑의 들쭉날쭉함(bankWobble)은 그대로다** —
  글자 S처럼 매끈해지면 도형으로 보인다는 이전 판단은 유효하다.
  폭은 원근(아래가 굵음) × 여울·소(폭이 일정하지 않음), 둑은 좌우가 따로 들쭉날쭉(`water.bankWobble`). 매끈한 리본으로 되돌리지 말 것.
  경로는 `config.river.path`로 코드 수정 없이 덮어쓸 수 있다(`river.setPath`).
  **폭 0.28**(2026-09-01 "물길을 더 키워 S자로" 요청, 0.21 → 0.28). 폭·굽이·소는 탐색으로 골랐다 — 띠 경계 0.032~0.973(무대 안), 달수 상한 158px(≥144).
  폭을 키우면 같이 움직여야 하는 값: `tributaryWidthRatio`(합류부 굵기 점프 ≤1.8), `natureOffset`(둑에서의 거리), `scene.minDist`(수관 침범 방지). 이번에 0.05 / 0.21 / 0.34.
  나무도 같이 줄인다(2026-09-01 "강이 커지니 나무를 먹는다"): `treeSize` 0.115→0.095, `treeCount` 30→24, `minDist` 0.36,
  `drawTreeAsset` 폭 상자 1.25→1.0(버드나무처럼 넓은 자산이 강까지 덮었다).
- **4줄기 합류**(기획 4·5번): "4개의 물길이 **위에서 아래로 하나로 뭉쳐지면서** S자 형태로 간다."
  상단 줄로 올라간 각 목표 문구 아래에서 물줄기가 나와 **물길 머리(u=0) 한 점**으로 모인다(`river.tributaries(sources, pointAt(0))`).
  합류점을 물길 **중간**에 두면 지류가 본류를 가로질러야 해서 합쳐지는 느낌이 사라진다 — 반드시 머리여야 한다.
  본류는 합류점(u=0)에서 하류로 **한 방향**으로 그려진다. 지류는 자연 회복 단계에 가서야 본류에 흡수된다(그 전에 지우면 "없어지고 딴 게 생겼다"로 보인다). 합류 순간 목표 문구 4개는 **사라지지 않고 상단 한 줄(`screen.goalRowTop`)로 올라가 남는다** — 시안 5컷의 "4개의 목표가 모여 하나의 깨끗한 물길이 됩니다"는 4개가 보여야 성립한다. 자연 회복(6컷)으로 넘어갈 때 페이드아웃.
- **자연 회복은 달수의 진행에 종속된다**: `anim.life = (swimP - 0.06) × 1.18` 로 달수보다 살짝 뒤처진 '생명의 앞머리'를 만들고, 자연 요소와 나무를 그 값으로 등장시킨다. 숲 슬롯은 **y 오름차순으로 정렬**해야 위→아래(달수가 가는 방향)로 살아난다.
  NATURE 단계는 자연을 피우는 구간이 아니라 **달수가 물길 머리에 떠오르는 짧은 비트**다(`anim.enter`).
  ⚠ 물을 탁하게 했다가 맑아지게 하는 연출은 **넣지 말 것** — 5컷 문구가 "하나의 **깨끗한** 물길이 됩니다"라 정면으로 모순된다(2026-08-27 시도 후 철회).
- **실사 자연 자산**(2026-08-27): `kiosk/assets/` 에 **종류별로** 넣는다 — `fish-*.png`(물 위) / `plant-*.png`(물가) / `tree-*.png`(숲).
  한 묶음(`nature-*`)으로 두면 물가에 물고기가 놓인다. 파일이 없으면 `art.js` 벡터 스프라이트로 자동 폴백.
  **물고기는 달수를 가리지 않는다** — `drawNature(..., avoid)` 가 달수 반경 안의 물고기를 비켜준다.
  현재 물고기 4·나무 4·물가 식물 3 모두 **생성형 AI 컷아웃**(2026-09-01, 동화풍 셀셰이딩). Wikimedia 실사 물고기는 톤이 안 맞아 교체했다.
  물고기 자산은 **오른쪽 보기** 가 기본 — renderer 가 흐름 방향에 따라 좌우 반전한다. 생성본이 세로면 `cutout-nature.py --fish-rot 90`.
  ⚠ 외부 이미지를 추가하면 반드시 `scripts/cutout-web-assets.py` 로 처리하고 `docs/ASSET_CREDITS.md` 에 출처·라이선스를 남긴다. 테스트가 이를 검사한다.
- **숲과 언덕**(시안 5·6컷): 물길만 있으면 강이 흰 배경에 떠 있다. 원경 언덕은 물길과 함께(RIVER), 나무는 자연 회복(NATURE) 때 하나씩 자란다.
  나무 자리는 `river.scenerySlots()` 가 **물길에서 `scene.minDist` 이상 떨어진 곳**만 고른다(수관이 강을 침범하면 안 됨). 배치는 결정적이라 현장에서 매번 같은 그림이 나온다.
  갈대는 물가 전용이라 숲 목록(`ART_TREE_ORDER`)에 넣지 않는다 — 넣으면 공중에 뜬다.
  카드에도 같은 나무를 양 가장자리에 세워 화면과 카드를 한 세계로 묶는다(`card.trees`).
- **강폭은 원근**: 먼 쪽(위)이 가늘고 가까운 쪽(아래)이 굵은 **단조 증가**(`water.widthAt`). 종 모양(가운데가 최대)은 원근 위반이라 "강"이 아니라 "리본"으로 읽힌다 — 되돌리지 말 것.
- **좌표계 규칙 (중요)**: `getBoundingClientRect()` 는 **뷰포트 절대 좌표**다. `#fx` 캔버스와 `#stage` 안의 요소는 **무대 좌표계**를 쓰므로 반드시 무대 좌상단 오프셋을 빼야 한다 — `renderer.js` 의 `elCenter()` 를 쓸 것.
  세로 키오스크에서는 오프셋이 0이라 티가 안 나지만, **가로 모니터에서는 무대가 가운데 레터박스로 놓여 물줄기·손가락 커서·물방울 터짐 좌표가 전부 화면 밖으로 밀린다.**
  `npm run build:demo` 의 **e2e 검증**(세로·가로 2회, 각 회차마다 **관람객 2명분 연속 진행**)이 이걸 잡는다 — 발원지가 화면 안인지, 메뉴 순서대로인지, 화면 폭에 고르게 퍼졌는지, 그리고 **실제 픽셀에 물이 그려졌는지**까지 본다.
- **성능 (GPU 없는 현장 PC 대비, 2026-08-27)**: 물 연출은 소프트웨어 렌더링에서 가장 무겁다. 아래를 되돌리지 말 것.
  · `fx` 캔버스를 **매 프레임 재할당하지 않는다**(크기·배율이 바뀔 때만). 1080×1920이면 프레임마다 8MB를 다시 잡는 셈이라 가장 비쌌다.
  · `ctx.filter = blur()` 는 매번 전체 서피스를 필터링한다. 저품질에서는 `water.softStroke()`(알파 낮춘 다중 스트로크)로 대체한다.
  · `PERF_STEPS` 4단계 자동 강등: 기본 → 흐림 끔(44구간) → 렌더 배율 0.6 → 0.45. **프레임 평균 28ms 초과가 1.5초 이상 지속되면 한 칸씩 내려간다.**
  · 시작 로그에 `그래픽 가속: 하드웨어/소프트웨어` 와 `렌더 성능 {fps, step, scale}` 이 남는다 — 현장 진단의 첫 단서.
  · `config.river.quality`: `auto`(기본) / `high` / `low`(2단계로 고정).
- **화면은 항상 9:16 무대 안에**: `#stage`가 `min(100vw, 56.25vh) × min(100vh, 177.78vw)` + `container-type: size`.
  무대 안 길이 단위는 뷰포트(`vw`/`vh`)가 아니라 **무대 기준(`cqw`/`cqh`)** 을 쓴다 — 가로 모니터에서 `vw`로 잡으면 물방울이 커지며 하단 달수를 덮는다. 인라인 스타일(JS)도 `cqw`/`cqh`.
  빌드 게이트가 `--smoke-emulate=1080x1920`(실기 해상도 CSS 레이아웃)과 `--smoke-size=960x540`(가로 비율)을 함께 검증한다.
  `--smoke-emulate` 는 창을 키우지 않고 뷰포트만 강제하므로, 개발 모니터가 작아도 실제 키오스크 레이아웃을 그대로 볼 수 있다.
- **손가락 커서는 가이드 단계 내내 보인다.** 4개를 다 눌러도 숨기지 않는다 — 가이드 화면이 넘어갈 때 섹션째 사라진다.
  물방울을 새로 만든 직후에는 레이아웃이 확정되기 전이라 좌표가 0일 수 있으므로 `requestAnimationFrame` 뒤에 배치한다.
- **SWIM 카메라**(2026-08-28): 시안 6컷은 달수 클로즈업이다. 지도 시점 그대로면 달수가 화면 높이의 9%뿐이라
  아무리 잘 움직여도 "고급"으로 안 읽힌다. `motion.swimCamera(p)` 로 **진행도 기반**(시간 기반 금지 — 스모크의 시간 압축에서
  줌이 안 걸린다) 줌 커브를 만들고 `renderer.camApply/camTick` 이 `#fx` 에 건다. 복귀는 COUNTDOWN 의 `readyMs` 동안(새 타이머 0개).
  · 변환 변수명은 반드시 **`z`**, PERF 단계 키는 **`camZoom`** — `sc`/`scale:` 로 쓰면 `storyboard.test.js` 회귀 가드에 걸린다.
  · 평행이동은 **정수 스냅**. 안 하면 구워 둔 레이어가 부분픽셀 리샘플링돼 프레임마다 미세하게 흔들린다.
  · 중심은 줌과 같은 비율로 화면 중앙에서 벗어나야 `z=1` 에서 정확히 항등변환이 된다(따로 두면 줌이 풀릴 때 화면이 밀린다).
  · 중심 추종 스무딩(`followMs`)에 반드시 **`ms()`** 를 곱한다. 안 하면 스모크에서 카메라가 목표를 못 따라간다.
    스무딩 없이 달수에 딱 붙이면 달수가 못 박히고 **세계가 스트로크마다 덜컹거린다**.
  · 물길 정적 캐시는 **고정 배율 `SHELL_Z` 로 한 장만** 굽고 대기 화면에서 미리 굽는다(1080×1920·2.2배 = 1963×2900, 65MB, 28ms 실측).
    라이브 줌을 넘기면 프레임마다 다시 굽는다. 줌이면 구간 수도 `RIVER_SEG_Z`(520)를 써야 곡선 바깥 이음매가 안 보인다.
  · `river.textureScale: 2` — 수면 텍셀 밀도가 이미 0.83/px 라 당기면 뭉갠다. **`tilePx` 는 건드리지 않는다**(늘리면 강이 바다가 된다).
  · 자연 슬롯은 `natureSlots(..., uTo)` 로 **달수가 실제로 가는 구간(u≤0.58)** 에만 놓는다. 예전 8개 배치는 절반이 하류에서 피어 안 보였다.
  · **현재 꺼져 있다**(`enabled: false`, 2026-08-29). 2.2 → 1.5 로 낮췄다가 결국 "확대가 될 필요가 없다"는 판단.
    자연을 입체 자산으로 바꾼 뒤로는 **그 세계를 통째로 보여주는 것**이 달수를 키우는 것보다 낫다.
    코드는 그대로 남아 있으니 `enabled: true` 한 줄로 되살릴 수 있다(줌 값은 1.5 로 저장돼 있다).
  · ⚠ **자연 밀도·나무 수는 카메라와 분리해 `config.scene` 에 둔다**(natureCount/natureUTo/natureSize/treeCount).
    카메라 스위치에 묶어 두면 카메라를 끌 때 경관까지 빈약해진다 — 실제로 그럴 뻔했다.
  · 되돌림: `config.swim.camera.enabled: false` → 예전 화면과 픽셀 동일(실측 평균차 0.26 < 노이즈 바닥 0.63).
- **헤엄 스프라이트 시트**(2026-08-28): 달수를 정지 PNG 한 장으로 돌리면 "뻣뻣한 스티커"로 읽힌다(현장 지적).
  `dalsu-swim.png`+`.json` 이 있으면 프레임 애니메이션으로, **없으면 지금까지와 100% 동일하게** 단일 이미지로 간다.
  · **프레임 인덱스는 반드시 위상에서 뽑는다**: `i = floor(((θ/2π + phase0) mod 1) × N)`, `θ = swimP × SWIM_STROKES × 2π`.
    시간 기반으로 따로 돌리면 몸짓과 전진·물 밀기가 어긋난다. `phase0` 는 검증 스크립트가 '가장 세게 젓는 프레임'을 찾아 넣는다.
  · 시트가 몸 굽이침을 담고 있으므로 절차 자세(`roll`/`sx`/`sy`/`bob`)는 0.35~0.6배로 감쇠한다. **`push`·`sink` 는 손대지 않는다**(물 반응 위상 규약).
  · 시트는 로드 시 **반드시 캔버스로 한 번 굽는다**. PNG 를 그대로 쓰면 첫 프레임 디코드가 8.9ms 까지 튀어 PERF 가 성능 부족으로 오인해 물 연출을 강등시킨다.
  · **카드는 시트를 쓰지 않는다**(`{ sprite: false }`) — 인쇄 화질과 "카드는 안 깨진다" 불변식 때문.
  · 메타는 `main.js` 의 `asset:swimMeta` IPC 로 읽는다. `file://` 페이지에서는 로컬 JSON 을 `fetch` 로 못 읽는다(opaque origin).
- **헤엄 시트 자산 파이프라인**: 플레이트(크로마 그린 `#00B140`) → 영상 생성(`start_image = end_image` 로 루프 강제) →
  `grab-frames.js`(균등 간격 **시킹**) → `key-swim-sheet.py` → `verify-swim-sheet.py`. 다음은 전부 실패해서 바꾼 것들이다.
  · 프롬프트에 **"swimming" 을 쓰지 않는다** — 모델이 물을 그리고, 파란 물은 녹색 키를 통과해 살아남는다. 동작만 기술한다.
  · 프레임 추출을 저속 재생 + `requestVideoFrameCallback` 으로 하면 **PNG 인코딩 동안 프레임이 지나간다**(107장 드롭). `pause()` 로도 안 막힌다.
  · 사이클 검출은 이음매만 보면 두 번 틀린다 — ① 배수 주기(3주기)를 고르면 앨리어싱이 나고,
    ② `start_image = end_image` 클립은 앞뒤가 정지 포즈라 **그 정지 구간이 항상 이겨** 24장이 전부 같아진다(실제로 그렇게 나갔다).
    점수를 `이음매 ÷ 구간내 움직임` 으로 두고, 움직임이 전체 평균의 60% 미만인 구간은 후보에서 뺀다.
  · 프롬프트에 **앞발 젓기를 명시**해야 헤엄으로 읽힌다. 꼬리만 흔드는 클립은 "수영하는 느낌이 안 난다"는 지적을 받았다.
  · 실루엣 IoU·면적·색분포의 한계는 **붕괴 감지선**이지 "움직이지 마라"가 아니다. 좁게 잡으면 우리가 원한 젓기 동작이 실패로 잡힌다.
    "여전히 달수인가"는 **원본 실루엣 정합 IoU**(≥0.80)가 직접 잰다.
  · 몸 상자는 알파 bbox 가 아니라 **원본 실루엣 정합**으로 잡는다. bbox 를 쓰면 꼬리가 펴진 만큼 종횡비가 1.95 가 되어 캐릭터가 눌린다.
  · 플랫 룩 판정에 '상위 8색 점유율'을 쓰지 말 것 — 그건 영상 압축 잡음을 센다(원본 0.96 vs 시트 0.68 인데 눈으로는 둘 다 플랫).
    원본과 같은 절차로 **면 내부 밝기 기울기**를 재 비율로 본다(실측 1.02배).
  · 무게중심·면적·색 분포는 **꼬리를 뺀 몸통 코어**에서 잰다. 실루엣 전체로 재면 우리가 원한 꼬리 움직임 자체가 실패로 잡힌다.
- **4대 목표 성과 한 줄**(2026-08-29) — **현재 꺼져 있다**(`config.screen.goalPayoff: []`, 2026-08-31).
  시안에 없는 문구라 "시안에 없는 텍스트가 많다"는 지적을 받고 뺐다. 코드는 남아 있으니 **머그음 문구 승인을 받으면 config 로 되살린다**.
  아래는 되살릴 때의 규칙이다.
  기획 목적은 어린이가 "왜 이 4가지를 해야 하는지"를 보는 것이다.
  달수가 지나가며 **화면에 실제로 나타나는 것**(물길 → 물고기 → 풀·꽃 → 숲)과 목표를 짝지어 한 줄씩 띄운다.
  · 문구·시점은 전부 `config.screen.goalPayoff` 에서 온다(`{ key, at, text }`, at = swimP). 코드에 하드코딩 금지.
  · DOM(`#story-goal`)이라 카메라 줌과 무관하게 항상 또렷하다. 순서는 자연 요소가 나타나는 순서에 맞춰야 의미가 산다.
  · **문구는 머그음 확인 대상**이다 — 어린이 대상 카피라 임의로 바꾸지 말 것.
- **자연 자산은 입체감 있는 컷아웃으로**(2026-08-29): 평평한 벡터 나무·수풀은 "2D로만 보인다"는 지적을 받았다.
  `tree-*.png`/`plant-*.png` 슬롯에 파일을 넣으면 벡터 폴백을 자동 대체하므로 **코드 변경 없이 룩만 갈아끼운다**.
  · 나무는 밑동 기준·원본 비율, 식물은 512 정사각 중앙 배치(물고기와 동일) — `scripts/cutout-nature.py` 가 그 규약대로 굽는다.
  · 컷아웃은 **색상 키잉이 아니라 테두리 flood fill** 이다. 잎이 초록이라 색으로 가르면 잎의 32%가 배경으로 잡혀 사라진다(실측).
    잎에 둘러싸인 배경은 테두리와 안 이어져 남으므로, '배경색 그대로인 평평한 구멍'만 따로 지운다.
  · 언덕도 단색 띠를 버리고 세로 그라데이션 + 능선 하이라이트를 준다. 그라데이션은 도형당 한 번이라 비용이 사실상 0이다.
- **수면은 급류가 아니라 시냇물**(2026-08-29): `makeWaterTexture` 의 대비를 2.7배로 늘리고 물마루를 순백(255)까지
  올렸더니 흰 물살이 화면을 덮어 급류로 읽혔다("물도 좀 자연스럽게"). 되돌리지 말 것:
  · 대비 1.75배, 물마루 지수 4.6(가늘게)·계수 0.42(약하게).
  · 물마루가 올라가는 색은 **순백이 아니라 `WATER_PALETTE.sheen`(226,242,250)** — 순백이면 거품이 된다.
  · 반사광 줄기와 물가 포말도 함께 낮췄다(0.13→0.075, 0.62→0.42). 기획 5컷 문구가 "깨끗한 물길"이지 "거센 물살"이 아니다.
- **촬영 프레이밍**(2026-08-29): 웹캠은 1280×720(가로 1.78)인데 카드는 664×1040(세로 0.64)이라
  cover 로 채우면 **화면 가로의 36%(459/1280)만** 잘라 쓴다 — 인물이 2.8배로 확대돼 얼굴이 카드를 덮는다.
  해상도를 올려도 비율 문제라 그대로다. `compose.photoCrop(..., card.photoZoom)` 이 더 넓게 잘라 **위쪽 띠**로 앉히고
  남는 아래는 배경 그라데이션 + 자연이 채운다. `photoZoom: 0.62` → 인물이 38% 작아지고 사진이 카드 높이의 62% 를 쓴다.
  · `photoZoom: 1` 은 예전 cover 와 **비트 단위로 동일**하다(반올림으로 1px 틈이 나지 않게 명시 처리).
  · **화면 미리보기도 같은 프레이밍**이어야 한다 — `#cam` 을 `--cam-h` 상단 띠로 두고 `fitCamPreview()` 가 같은 슬라이스 종횡비로 계산한다.
    안 맞추면 관람객이 화면에서 본 구도와 인쇄물이 달라진다.
- **카드 나무는 화면과 같은 자산을 쓴다**: `A.tree` 가 있으면 그것, 없으면 벡터. 여기만 벡터로 두면
  물가 식물은 실사 컷아웃인데 나무만 납작해 **한 장에 두 화풍이 섞여 인쇄된다**(실제로 그렇게 뽑혔다).
  나무는 `drawTreeAsset()` 으로 **상자 안에 맞춰** 넣는다 — 높이만 맞추면 가로로 넓은 덤불이 폭 1.8배로 그려져 화면·카드를 혼자 덮는다.
- **데모 영상 녹화**(`--record`, 2026-08-31): 실제 앱을 그대로 돌려 `out/demo/*.mp4` 로 담는다.
  자동 진행은 스모크 구동부를 그대로 쓰고, **촬영 화면 직전(헤엄 도착)에서 멈춘다** — 웹캠 화면은 담지 않는다.
  · OS 창 캡처(desktopCapturer + MediaRecorder)를 먼저 썼다가 **버렸다**: 윈도우에서 정지 화면이 담기고(21초 내내 대기 화면),
    창 테두리·여백이 들어가고, 대상 창 매칭이 실패하면 조용히 **다른 창(VS Code)이 녹화됐다**.
  · 지금은 `webContents.beginFrameSubscription` 으로 받은 프레임을 ffmpeg(rawvideo bgra)에 밀어 넣는다.
    창 밖의 것이 섞일 수 없고 크기가 창 내용과 정확히 같다.
  · 프레임은 **벽시계로 채운다**. 타이머 호출 횟수를 세면 메인이 밀릴 때 21초가 15.6초로 기록돼 26% 빨리 재생된다(실측).
  · 출력은 H.264 + yuv420p + faststart **MP4**. WebM 은 헤더에 길이가 없어 시킹이 안 되고 카톡·기본 플레이어에서 재생이 안 된다.
  · 녹화 중에는 `snap()`(capturePage)을 건너뛴다 — 프레임마다 수십 ms 를 먹어 녹화가 끊긴다.
  · `ffmpeg-static` 은 **devDependency** 라 배포본에는 안 들어간다(`require` 는 try/catch).
- **방향 전환은 한 프레임에**(2026-08-31): S자 물길은 진행 방향이 총 160° 돌아간다(u=0 에서 171°, u=0.5 에서 10°).
  스프라이트가 왼쪽을 보고 누운 그림이라 이때 **좌우 반전**이 필요하다(180° 회전은 배가 하늘로 간다).
  · 처음엔 폭을 0 으로 지나가며 보간해 '몸을 트는' 그림을 의도했다. **틀렸다** — 실제로는 몸이 세로로 납작하게 눌렸다가
    반대로 펴져 뒤집는 것처럼 보였고 현장에서 "보통 뒤집어서 수영하진 않는다"는 지적을 받았다. 수달은 물에서 몸을 뒤집지 않는다.
  · 지금은 **`face` 가 ±1 뿐이고 한 프레임에 바뀐다.** 방향이 바뀌는 지점은 물길이 거의 수직인 곳이라
    그때의 실루엣이 좌우 대칭에 가까워 '굽이를 돌았다'로 읽힌다. 기울기도 그 프레임에서 함께 스냅한다
    (지연을 두면 '오른쪽을 보는데 몸은 왼쪽으로 누운' 그림이 된다).
  · 전환 프레임은 물보라로 덮는다 — 단 **링을 만드는 splash/burst 를 쓰지 말 것**. 퍼지는 흰 링이 물길 밖 잔디에 찍혀
    "CG 도형"으로 읽힌다(이미 한 번 겪었다). 물방울만 튀기는 `particles.spray()` 를 쓴다.
  · **몸 회전은 ±22°(`swimTilt` 감쇠 0.5, 클램프 0.38rad)**. 물길 방향을 그대로 따라가게(감쇠 1.0) 했다가 되돌렸다 —
    시트는 **앞에서 본** 누운 그림이라 90° 까지 돌리면 공중제비처럼 읽힌다("수영이 막 뒤집어진다", 2026-08-31).
  · 세 가지(고정 크기 / 안 돌림 / 물길 안)는 S자의 세로 구간에서 양립하지 않는다 — 가로로 누운 몸(폭=높이×1.41)이 강폭을 넘는다.
    달수를 돌리지도 줄이지도 않고 **강 쪽을 넓혀** 푼다: `config.river.pool {u,w,k}` 가 굽이에 소(웅덩이)를 만든다(`water.widthAt` 3번째 인자).
    실제 강도 굽이 바깥에 소가 생기므로 자연스럽다. 이걸 빼면 굽이에서 84px 까지 쪼그라드는 문제가 되돌아온다.
  · 방향 전환(좌우 반전)은 한 프레임에 바꾸되 **0.4초 디졸브**로 잇는다(`anim.flipAt`, 사라지는 쪽은 `noFx` 로 몸만 그린다).
    폭을 0 으로 눌러 보간하는 방식은 뒤집는 것처럼 보여 폐기했다.
  · 물길 머리(처음 ~25%)에서는 강이 좁아 105→144px 로 살짝 자란다. 떠오르는 도입부라 두었다. 없애려면 `minTaper` 를 올려야 하는데 합류부 모양이 바뀐다.
  · **달수 크기는 고정**(`config.swim.dalsuHeight: 0.075`, 화면 높이 비율). 물길 폭에 비례시켰을 때는 상류에서 62% 까지
    작아져 '멀리서 다가오는' 원근이 걸렸는데, 시안 6컷은 접근 샷이 아니라 그냥 헤엄치는 그림이다.
    폭 제한(`bandW*0.92*bend/crossSpan`)은 남아 있어 물길 머리 처음 10% 에서만 살짝 작다(떠오르는 도입부라 자연스럽다).
    그 폭 제한을 없애지 말 것 — 물길보다 커지면 잔디 위에 얹힌 것처럼 보인다.
- **바닥 결**(2026-08-31): 물길이 굽는 안쪽에 넓은 단색 잔디가 생겨 '색종이'로 보였다.
  `MEADOW` — 밝기만 살짝 다른 납작한 타원 44개를 R2 수열로 결정적으로 흩뿌린다. 그라데이션·blur 를 안 써서 프레임 비용이 사실상 0(숲언덕 0.4ms 유지).
- **24시간 무인 운영 장치**(2026-09-01 리뷰로 추가) — 되돌리지 말 것:
  · `fxLoop` 는 `fxFrame()` 을 try/catch 로 감싸고 **무슨 일이 있어도 다음 프레임을 예약**한다. 예외가 한 번 새면 rAF 재등록이 안 돼 화면이 영원히 멈춘다.
  · `render-process-gone` → 3초 후 reload, `unresponsive` → 15초 뒤에도 그대로면 강제 재시작. `powerSaveBlocker` 로 모니터 절전 차단.
  · 웹캠 트랙 `ended` → 5초 간격 재연결. 촬영 시점에 카메라가 죽어 있으면 회색 실루엣을 인쇄하지 않고 오류 화면으로 간다.
  · 로그 파일명은 **매 기록마다** 오늘 날짜(`logFile()`)로 계산한다 — 시작 시 고정하면 자정을 넘긴 첫날 파일이 무한히 커진다.
  · `config.output.keepDays` 를 실제로 구현했다(`pruneOld`, 시작 시 + 6시간마다, 날짜 폴더·로그만). 예전엔 config 에만 적혀 있고 아무 데서도 안 쓰였다.
  · 인쇄 중(`printerBusy > 0`)에는 30초 주기 프리플라이트 `--list` 를 건너뛴다 — 같은 SmartComm2 를 두 프로세스가 동시에 건드리지 않게.
  · 소크 테스트: `npx electron kiosk/main.js --smoke-soak=40` — 관람객 40명분을 연속으로 돌리며 JS 힙과 프로세스 메모리를 기록하고,
    뒤 1/4 평균이 앞 1/4 대비 20% 넘게 오르면 실패. 2026-09-01 실측: JS 힙 9.5MB 평평(growth 1.00), 전체 프로세스 작업세트 563→516MB(증가 없음).
- **데드맨 스위치**: 어떤 상태든 예산의 2배(최소 20초) 동안 진행이 없으면 대기 화면으로 강제 복귀. `idleReturnMs`는 GUIDE에서만 동작하므로 그 밖의 상태를 이게 덮는다.
- 촬영 직전 `timing.readyMs` 동안 "준비하세요"를 띄운다. 곧바로 카메라를 켜면 놀란 표정으로 찍힌다.
- 강물은 `water.js`의 **흐르는 수면**(텍스처를 흐름 방향으로 스크롤). 흰 테두리 + 점선 대시 스트로크는 유아틱해서 폐기 — `config.river.style: "cartoon"`으로만 되돌릴 수 있다.
  수면 텍스처는 절차 생성이 기본이고, `kiosk/assets/water.png`(흐름 방향 = 가로, 좌우 타일러블)를 넣으면 그 이미지로 교체된다.
  **현재는 실사풍 `water.png` 를 쓴다**(2026-09-01, "진짜 깨끗한 물처럼" 요청). `scripts/make-water-tile.py` 가 생성 원본을 좌우 블렌딩(이음매 평균차 2.6/255)하고
  청록으로 재채색한다 — 생성본 그대로는 모래 바닥 위 얕은 물이라 거의 흰색이어서 강이 회색 리본으로 읽혔다. 되돌리려면 파일만 지운다.
- 물방울은 **가로 1열 4개, 눈물방울 모양**. 터치하면 물방울이 사라지고 **그 자리에 목표 문구가 남는다**(누적, 시안 3~4컷).
- 달수 **헤엄 위상은 전진 위상과 같은 값**을 쓴다: `θ = swimP × SWIM_STROKES × 2π`. 시간 기반으로 따로 돌리면 몸짓과 이동이 어긋나 허우적거려 보인다.
  더 나아가 **물 차는 힘의 위상까지 맞춰야 한다**: `swimEase` 의 속도는 `1-0.6cos(θ)` 라 **θ=π 에서 최대**이므로 `push = max(0, -cos(θ))`.
  `sin(θ)` 를 쓰면 90° 어긋나 "가장 빨리 나아가는 순간에 몸은 쉬고 있는" 그림이 된다. `tests/storyboard.test.js` 가 이 위상 일치를 검사한다.
  달수는 **물길 폭 안에 들어와야** 한다 — 물길보다 크면 물길을 덮어 잔디 위에 있는 것처럼 보인다(`river.widthRatio` ↔ 달수 크기 함께 조정).
- 달수 이동은 **헤엄**(motion.js `swimEase`/`swimPose` — 차고-미끄러짐 + 몸통 흔들림). 등속 슬라이드 금지.
  도착점은 `config.swim.arriveU`(현재 **0.82**, 화면 아래 y≈0.92). 기획 7번의 '중앙(0.5) 도착'에서 "끝까지 내려오게" 요청으로 바꿨다(2026-09-01).
  하류에도 세로 굽이(u≈0.72)가 있어 `river.pool` 이 **배열**로 소를 둘 둔다. `swimMs` 도 6000→8000 으로 늘렸다(거리 1.7배, 스트로크 리듬 유지).
- **카드 앞면 AR = 「복구된 자연(강물 제외) + 달수」만**(2026-08-26 확정). 강물·수면은 화면 연출 전용이며 카드에 넣지 않는다.
  자연은 `compose.natureCardSlots()` 의 세 구역(왼쪽 물가 / 인물 앞 전경 / 달수 왼쪽 틈)에만 앉히고, 하단에 전경 풀을 깐다.
  피해야 할 것: ① 가운데 인물(얼굴·상반신) ② 우하단 달수. 나는 것(잠자리)은 접지 그림자를 그리지 않는다.
- **물방울은 2단계**(2026-09-03 클라이언트 요청): 첫 화면은 **제목 + Reduce/Reuse/Recycle 3개 + 달수 + 안내 문구**만(부제 `screen.headSub` 는 빈 문자열, renderer 가 칸째 숨김).
  셋을 다 누르면 `T.goalTextMs` 뒤 `revealStage2()` — 세 문구가 상단 줄(`goalRowTop`, 4칸 슬롯 `ROW_X`)로 올라가고 **Return 물방울(`goals[].stage: 2`)이 가운데 맺힌다**.
  Return 을 누르면 그 문구도 4번째 슬롯으로 올라가고 지금까지와 같은 **4줄기 합류 → S자 → 달수 헤엄**이 이어진다. `config.goals` 는 여전히 4개(main.js 가 4개를 강제).
  · 물방울 자리는 **같은 단계 안에서** 가운데 정렬(`POS`), 지류 발원지 x 는 물방울 자리가 아니라 상단 줄 슬롯 `ROW_X` 다 — 3개 정렬과 4칸 줄의 x 가 다르기 때문.
  · `raiseGoalTexts()` 는 **터진(popped) 문구만**, 이동량은 변형 없는 물방울 중심 기준으로 잰다(두 번 불려도 누적되지 않는다). 2단계 공개와 합류 직전에 각각 한 번씩 불린다.
  · 숨은(`.staged`) 물방울은 손가락 커서가 가리키지 않는다 — display:none 요소의 rect 가 0 이라 커서가 좌상단으로 튄다.
- **삼성 지정 팔레트**(2026-09-03 클라이언트 전달, "이 색상 위주로") — **물방울 `#00b3e3` · 나무/숲 `#00c3b2` · 파랑 계열 `#0077c8`**. 되돌리지 말 것.
  · CSS 는 `:root` 의 `--s-drop / --s-forest / --s-blue / --s-blue-dark(#005a9e)` 에서만 색을 뽑는다. 제목·캡션·목표 문구·버튼은 `--s-blue`, 부제·아이콘은 `--s-blue-dark`.
  · `config.goals[].color` 4개 모두 `#00b3e3`(물방울은 한 색), `iconColor` `#005a9e`. 배경 하늘 그라데이션(index.html)은 `#00b3e3` 의 30/15/6% 틴트.
  · 수면: `make-water-tile.py` 가 `#0077c8`(깊은 물) ↔ `#00b3e3` 흰색 50% 틴트(얕은 물)로 재채색. 절차 폴백 `WATER_PALETTE` 도 같은 값.
  · 숲: 언덕 3밴드·바닥 결·카드 전경 풀·벡터 폴백 나무는 `#00c3b2` 의 틴트/셰이드(`#00907f`·`#00a897`·`#22cdb9`·`#4fd6c8`).
    **PNG 컷아웃(`tree-*`/`plant-*`)은 `scripts/recolor-nature.py` 로 잎 구간만 색상각 175° 로 옮겼다** — 줄기·꽃은 그대로. 자연 자산을 새로 컷아웃하면 이 스크립트를 다시 한 번 돌린다(한 번만).
  · 물고기(`fish-*`)와 달수 원본은 재채색하지 않는다(캐릭터·생물 고유색).
- **카드 뒷면은 클라이언트 최종 디자인**(2026-09-03): 원본 `assets-src/card-back-final.png`(685×1063) → `make-card-back.ps1` 이 664×1040 cover 로 맞춰 `kiosk/assets/card-back.png` 생성.
  최종본 파일이 있으면 `npm run assets` 가 플레이스홀더를 그리지 않는다. 뒷면을 바꿀 때는 `assets-src/card-back-final.png` 만 교체하고 스크립트를 돌린다.
- **디자인은 진하게, 글씨는 볼드**: 서체는 삼성 SS Head(제목)/Body(본문)이며 진짜 700 웨이트가 있다(2026-09-01). 그 전엔 Jua 합성 볼드 + 외곽 보강이었다.
  Jua/Gowun Dodum 으로 되돌리지 말 것 — 클라이언트 요청으로 삼성 서체 전면 적용이다. 물방울 라벨은 흰색(진한 물방울 위 대비), 목표 문구는 삼성 파랑 `--s-blue`(#0077c8) + 흰 외곽(2026-09-03, 그 전엔 #063a5c).
- `BrowserWindow`는 `backgroundThrottling: false` 필수. 켜져 있으면 창이 가려질 때 rAF가 멈춰 연출이 정지한다.
- **촬영 화면(COUNTDOWN·CAPTURE)은 비운다**: 카메라 위에 달수·물길·자연을 겹치지 않는다(사람이 가려짐). AR 합성은 출력 카드에서만. `screen.captureOverlay: true`로만 예외 허용.
- **뷰파인더**(`config.screen.viewfinder`, 2026-09-01, 시안에 없음 — 머그음 확인 대상): 촬영 화면에 **라이브 영상 + 인쇄될 합성(자연·달수)** 을
  인쇄물과 똑같이 겹쳐 보여준다. 폭 66% 틀로 띄웠더니 "어색하다"는 판단이라 **`width: 1.0`(카드가 곧 화면)** 으로 꽉 채운다 —
  카드 비율(664:1040)이 화면(9:16)보다 넓어 폭을 채우면 높이는 **88%** — 남는 12% 는 구조적이라 위아래로 나눠 가운데 두고(`VF.top` 생략 시 자동)
  `#stage` 배경을 카드 배경색으로 칠해 '빈 공간'이 아니라 여백으로 보이게 한다. ⚠ 그 색을 `#countdown`(.screen, 카메라 **위** 레이어)에 주면 카메라가 통째로 가려진다(실제로 그랬다).
  카운트다운 숫자는 얼굴을 가리지 않게 우상단 구석으로 옮긴다(`#stage.vf #count-num`).
  · **`card.photoZoom: 1.0`(2026-09-01 확정)** — "화면 전체가 카메라이고 자연·달수가 투명 PNG 처럼 얹히는" 그림을 원했다.
    0.62 는 사진을 위쪽 띠로 자르고 아래를 단색으로 메워서 그렇게 보이지 않았다. 대신 웹캠 가로의 36% 만 쓰므로 얼굴이 크게 나온다 —
    카드에서 과하면 0.8 정도로 절충하거나 웹캠을 뒤로 물린다(config 만으로 조정, 재빌드 불필요).
  · 인쇄(`composeCard`)와 오버레이(`paintViewfinderOverlay`)는 **같은 `drawCardLayers()`** 를 쓴다 — 보이는 그대로 인쇄된다. 두 곳을 따로 그리지 말 것.
  · 오버레이는 `photo:false` 로 사진 자리를 비워(투명) 라이브 영상 위에 얹는다. 촬영 화면 첫 프레임에 한 번 그린다(자산이 다 준비된 뒤).
  · 틀 주변은 강 장면을 `sceneAlpha`(0.32)로 옅게 남긴다(빈 여백 → 강가 배경). fx 캔버스가 `<video>` 위에 있으므로 **틀 안은 destination-out 으로 도려낸다**.
  · ⚠ `vfBox`/`vfPainted` 는 `var` 이고 **초기값을 대입하지 않는다**. `initCamera → fitCamPreview` 가 그 선언보다 먼저 실행되므로,
    `var x = null` 로 쓰면 먼저 세팅된 값을 null 로 덮어써 틀 도려내기·오버레이가 조용히 꺼진다(실제로 그랬다).
  · `enabled:false` 면 예전(전면 미리보기)으로 돌아간다. 얼굴이 카드에 너무 크면 이것과 별개로 `card.photoZoom` 을 낮춘다.

## 배포 · 자동 업데이트 (2026-09-01)
- **릴리스 = 태그**: `package.json` 버전을 올리고 `git tag vX.Y.Z && git push --tags` 하면 `.github/workflows/release.yml` 이
  테스트 → 인쇄 CLI 빌드 → 스모크 → 인스톨러/zip 빌드 → GitHub Release 업로드까지 한다. 태그와 package.json 버전이 다르면 실패한다.
- **키오스크는 설치형(NSIS) 으로 설치**한다(`DalsuARKiosk-Setup-X.Y.Z.exe`, 사용자별 원클릭). 설치형만 자동 업데이트가 된다 —
  zip/포터블 실행본은 수동 교체. 설치형의 **데이터 폴더는 `문서\DalsuARKiosk`**(config.json / out / logs) — 업데이트 때 설치 폴더가 갈려도 보존된다.
  시작 로그의 `data` 경로가 곧 현장에서 config.json 을 찾을 위치다. 설치 폴더는 `%LOCALAPPDATA%\Programs\dalsu-ar-kiosk`(패키지명 기준, 앱 이름 아님).
- **2026-09-01 E2E 실측**: 설치본 0.7.0 → CI 릴리스 v0.7.1 발견 17초 → 다운로드 18초(146MB) → IDLE 24초 후 재시작 → 0.7.1 기동, `config.json` 보존.
  CI(`release.yml`)도 첫 실행에 통과(테스트·인쇄 CLI 빌드·스모크 2종·업로드, 약 12분).
- 업데이트 규칙(`main.js setupAutoUpdate`): 시작 15초 후 + `config.update.checkMinutes`(60)마다 확인, 백그라운드 다운로드,
  **렌더러가 IDLE 로 `idleSeconds`(20) 이상 머물 때만** 재시작해 적용. 체험 도중엔 절대 끼어들지 않는다. 실패는 로그만 남기고 현재 버전 유지.
  끄기: `config.update.enabled:false` 또는 `--no-update`.
- 저장소는 행사 기간 **공개**로 전환했다(2026-09-01, 키오스크가 토큰 없이 Release 를 받기 위해). 행사 후 비공개로 되돌리려면
  자동 업데이트도 같이 끄거나 토큰 방식으로 바꿔야 한다.

## 명령
```bash
npm install                 # kiosk 의존성 (electron)
npm test                    # 순수 모듈 테스트 (node --test)
npm run smoke               # 웹캠 없이 전 흐름 자동 실행 → out/smoke/front.png + dry-run 인쇄 → exit 0
npm start                   # 키오스크 실행 (창 모드)
npm run kiosk               # 전체화면 키오스크 모드
npm run build:printer       # DalsuPrint.exe 빌드 (dotnet publish)
npm run assets              # .ai → PNG 변환(+파편 제거) + 뒷면 카드 생성
npm run assets:swim:plate   # 헤엄 사이클 생성용 시작 플레이트 (크로마 그린)
npm run assets:swim         # 프레임 → 스프라이트 시트 + 자동 검증 (일회성 오프라인 작업)
npm run record              # 데모 영상 녹화 → out/demo/*.mp4 (대기~헤엄, 촬영 화면 제외)
npm run release             # 로컬에서 빌드 + GitHub Release 업로드 (GH_TOKEN 필요; 보통은 태그 푸시로 CI 가 한다)
printer/dist/DalsuPrint.exe --list                       # 프린터 목록
printer/dist/DalsuPrint.exe --front a.png --back b.png   # 실제 인쇄
```

## 완료 게이트 (보고 전 필수)
1. `npm test` 전부 통과
2. `npm run smoke` exit 0 + `out/smoke/front.png` 1012×636 생성 확인
3. `npm run build:printer` 성공 + `DalsuPrint.exe --dry-run --front ... --back ...` exit 0
4. 실장비 인쇄는 Smart-81D 연결된 PC에서만 검증 가능 — 미검증 시 "미검증"으로 명시 보고
   · **2026-09-01 실장비 양면 인쇄 확인됨** (패키지 `pkg-0901-121616`, `printer.sdk: comm`). 이후 인쇄 경로(`DalsuPrint`·`printCard`·카드 규격)를 바꾸면 다시 실기 검증이 필요하다.

## 컨벤션
- JS: CommonJS, 순수 로직은 DOM 의존 없이 분리(테스트 가능). 한글 주석 OK.
- C#: 기존 NFC Writer 코드 스타일 유지. 로그는 stdout, 오류는 stderr + exit code(0 성공/1 인자/2 SDK/3 프린터/4 인쇄).
- 커밋: conventional commits. 자동 커밋 금지.
- 자산 파일명 고정: `dalsu-front.png`, `dalsu-side.png`, `dalsu-float.png`, `dalsu-face.png`, `card-back.png`,
  `dalsu-swim.png`+`dalsu-swim.json`(헤엄 시트, 선택). 그래픽 교체는 파일만 덮어쓴다.
